# Non-canonical zarith for timestamp: the canonical encoding of
# 12345 with a spurious trailing 0x00 group. timestamp decodes
# through the same zarith path as int, so it must also be rejected.
code { UNPACK timestamp } ;
input { Stack_elt bytes 0x0500b9c08100 } ;
output { Stack_elt (option timestamp) None }
