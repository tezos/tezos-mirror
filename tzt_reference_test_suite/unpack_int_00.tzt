# UNPACK int on the canonical multi-byte encoding of 12345 must
# succeed. Control case for the trailing-zero rejection in
# unpack_int_01..04.
code { UNPACK int } ;
input { Stack_elt bytes 0x0500b9c001 } ;
output { Stack_elt (option int) (Some 12345) }
