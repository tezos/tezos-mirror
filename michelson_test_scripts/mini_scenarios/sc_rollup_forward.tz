parameter (pair bytes (contract bytes)) ;
storage unit ;
code { UNPAIR ;
       DIP { NIL operation };
       UNPAIR ;
       DIP { PUSH mutez 0 };
       TRANSFER_TOKENS;
       CONS;
       PAIR; }
