# This contract sends to the address given as argument a
# freshly-created big map.  This contract is used to test temporary
# big map ids which are generated when big maps are transferred from
# one contract to another.  See also receiver_*.tz and the other
# sender_*.tz
parameter (contract (big_map string bytes));
storage unit;
code
  {
    UNPAIR;
    PUSH mutez 0;
    EMPTY_BIG_MAP string bytes;
    PUSH (option bytes) (Some 0x); PUSH string "d"; UPDATE;
    TRANSFER_TOKENS;
    NIL operation; SWAP; CONS;
    PAIR
  }
