# This contract drops the big map it receives as parameter.  This
# contract is used to test temporary big map ids which are generated
# when big maps are transferred from one contract to another.  See
# also sender_*.tz and the other receiver_*.tz.
parameter (big_map string bytes);
storage unit;
code { CDR; NIL operation; PAIR }
