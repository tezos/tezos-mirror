parameter int ;
storage nat ;
code { CAR;
       DUP;
       GT;
       LOOP { PUSH int 1; SWAP; SUB; DUP; GT; };
       DROP;
       STEPS_TO_QUOTA;
       # PUSH nat 1;
       NIL operation;
       PAIR;
     };
