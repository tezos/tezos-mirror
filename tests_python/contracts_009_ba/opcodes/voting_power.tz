parameter baker_hash;
storage (pair nat nat);
code { CAR;
       VOTING_POWER; # Get the number of rolls for the baker
       DIP { TOTAL_VOTING_POWER }; # Get the total number of rolls
       PAIR;
       NIL operation; PAIR; };
