# EVM Rollup configuration

This folder contains the different configuration for the EVM rollup. It describes
the `config.yaml` used by the `smart-rollup-installer` to compile the kernel with
predefined values.

