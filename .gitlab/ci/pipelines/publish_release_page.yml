# This file was automatically generated, do not edit.
# Edit file ci/bin/main.ml instead.

stages:
- start
- publish

datadog_pipeline_trace:
  image: datadog/ci:v5.18.0@sha256:07475bc65fc5efec461feeaca7a16cc32c13742393db6cce5352fc5f7e1bd5a2
  stage: start
  tags:
  - gcp
  needs: []
  dependencies: []
  allow_failure: true
  timeout: 60 minutes
  interruptible: true
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - CI_MERGE_REQUEST_IID=${CI_MERGE_REQUEST_IID:-none}
  - DATADOG_SITE=datadoghq.eu datadog-ci tag --level pipeline --tags pipeline_type:$PIPELINE_TYPE
    --tags mr_number:$CI_MERGE_REQUEST_IID
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'

grafazos.release-page-publish:
  image: ${GCP_PROTECTED_REGISTRY}/tezos/tezos/alpine-release-page:master-73a6a3d6
  stage: publish
  tags:
  - gcp_not_interruptible
  rules:
  - when: manual
    allow_failure: true
  needs: []
  dependencies: []
  timeout: 60 minutes
  interruptible: false
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - eval $(opam env)
  - ./grafazos/scripts/releases/publish_release_page.sh
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  variables:
    BUCKET_PATH: /releases
    DISTRIBUTION_ID: ${CLOUDFRONT_DISTRIBUTION_ID}
    S3_BUCKET: site-prod.octez.tezos.com
    URL: octez.tezos.com
  environment:
    name: release-page
    action: access
  artifacts:
    expire_in: 1 day
    paths:
    - ./index.md
    - index.html
  retry: 0

octez-smart-rollup-node.release-page-publish:
  image: ${GCP_PROTECTED_REGISTRY}/tezos/tezos/alpine-release-page:master-73a6a3d6
  stage: publish
  tags:
  - gcp_not_interruptible
  rules:
  - when: manual
    allow_failure: true
  needs: []
  dependencies: []
  timeout: 60 minutes
  interruptible: false
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - eval $(opam env)
  - ./scripts/rollup_node/releases/publish_release_page.sh
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  variables:
    BUCKET_PATH: /releases
    DISTRIBUTION_ID: ${CLOUDFRONT_DISTRIBUTION_ID}
    S3_BUCKET: site-prod.octez.tezos.com
    URL: octez.tezos.com
  environment:
    name: release-page
    action: access
  artifacts:
    expire_in: 1 day
    paths:
    - index.md
    - index.html
  retry: 0

release_page.publish:
  image: ${GCP_PROTECTED_REGISTRY}/tezos/tezos/alpine-release-page:master-73a6a3d6
  stage: publish
  tags:
  - gcp_not_interruptible
  rules:
  - when: manual
    allow_failure: true
  needs: []
  dependencies: []
  timeout: 60 minutes
  interruptible: false
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - eval $(opam env)
  - ./scripts/releases/publish-release-page.sh
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  variables:
    BUCKET_PATH: /releases
    DISTRIBUTION_ID: ${CLOUDFRONT_DISTRIBUTION_ID}
    S3_BUCKET: site-prod.octez.tezos.com
    URL: octez.tezos.com
  environment:
    name: release-page
    action: access
  artifacts:
    expire_in: 1 day
    paths:
    - index.html
    - older_releases.html
    - index.md
    - older_releases.md
  retry: 0

teztale.release-page-publish:
  image: ${GCP_PROTECTED_REGISTRY}/tezos/tezos/alpine-release-page:master-73a6a3d6
  stage: publish
  tags:
  - gcp_not_interruptible
  rules:
  - when: manual
    allow_failure: true
  needs: []
  dependencies: []
  timeout: 60 minutes
  interruptible: false
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - eval $(opam env)
  - ./teztale/scripts/releases/publish_release_page.sh
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  variables:
    BUCKET_PATH: /releases
    DISTRIBUTION_ID: ${CLOUDFRONT_DISTRIBUTION_ID}
    S3_BUCKET: site-prod.octez.tezos.com
    URL: octez.tezos.com
  environment:
    name: release-page
    action: access
  artifacts:
    expire_in: 1 day
    paths:
    - ./index.md
    - index.html
  retry: 0
