# This file was automatically generated, do not edit.
# Edit file ci/bin/main.ml instead.

eth.build:static-x86_64-linux-binaries:
  image: ${build_deps_image_name}:runtime-build-dependencies--${build_deps_image_version}
  stage: build
  tags:
  - gcp
  dependencies: []
  before_script:
  - ./scripts/ci/take_ownership.sh
  - eval $(opam env)
  script:
  - ./scripts/ci/build_static_binaries.sh
  variables:
    ARCH: x86_64
    EXECUTABLE_FILES: script-inputs/etherlink-executables
  artifacts:
    expire_in: 90 days
    paths:
    - octez-binaries/$ARCH/*

eth.build:static-arm64-linux-binaries:
  image: ${build_deps_image_name}:runtime-build-dependencies--${build_deps_image_version}
  stage: build
  tags:
  - gcp_arm64
  dependencies: []
  before_script:
  - ./scripts/ci/take_ownership.sh
  - eval $(opam env)
  script:
  - ./scripts/ci/build_static_binaries.sh
  variables:
    ARCH: arm64
    EXECUTABLE_FILES: script-inputs/etherlink-executables
  artifacts:
    expire_in: 90 days
    paths:
    - octez-binaries/$ARCH/*

eth.build:dpkg:amd64:
  image: $DISTRIBUTION
  stage: build
  tags:
  - gcp
  needs: []
  dependencies: []
  before_script:
  - . ./scripts/version.sh
  - export DEBIAN_FRONTEND=noninteractive
  - apt update
  - apt-get install -y rsync git m4 build-essential patch unzip wget opam jq bc autoconf
    cmake libev-dev libffi-dev libgmp-dev libhidapi-dev pkg-config zlib1g-dev libprotobuf-dev
    protobuf-compiler libsqlite3-dev jq
  script:
  - wget https://sh.rustup.rs/rustup-init.sh
  - chmod +x rustup-init.sh
  - ./rustup-init.sh --profile minimal --default-toolchain  $recommended_rust_version
    -y
  - . $HOME/.cargo/env
  - export OPAMYES="true"
  - opam init --bare --disable-sandboxing
  - make build-deps
  - eval $(opam env)
  - make $TARGET
  - DISTRO=$(echo "$DISTRIBUTION" | cut -d':' -f1)
  - RELEASE=$(echo "$DISTRIBUTION" | cut -d':' -f2)
  - mkdir -p packages/$DISTRO/$RELEASE
  - mv etherlink-*.* packages/$DISTRO/$RELEASE/
  variables:
    OCTEZ_PKGNAME: etherlink
    TARGET: dpkg
    OCTEZ_PKGMAINTAINER: nomadic-labs
    BLST_PORTABLE: "yes"
    ARCH: amd64
  artifacts:
    name: ${TARGET}-$ARCH-$CI_COMMIT_REF_SLUG
    expire_in: 1 day
    paths:
    - packages/
    when: on_success
  parallel:
    matrix:
    - DISTRIBUTION:
      - debian:bookworm
      - ubuntu:focal
      - ubuntu:jammy

eth.build:rpm:amd64:
  image: $DISTRIBUTION
  stage: build
  tags:
  - gcp
  needs: []
  dependencies: []
  before_script:
  - . ./scripts/version.sh
  - .gitlab/ci/jobs/build/bin_packages_rpm.sh
  script:
  - wget https://sh.rustup.rs/rustup-init.sh
  - chmod +x rustup-init.sh
  - ./rustup-init.sh --profile minimal --default-toolchain  $recommended_rust_version
    -y
  - . $HOME/.cargo/env
  - export OPAMYES="true"
  - opam init --bare --disable-sandboxing
  - make build-deps
  - eval $(opam env)
  - make $TARGET
  - DISTRO=$(echo "$DISTRIBUTION" | cut -d':' -f1)
  - RELEASE=$(echo "$DISTRIBUTION" | cut -d':' -f2)
  - mkdir -p packages/$DISTRO/$RELEASE
  - mv etherlink-*.* packages/$DISTRO/$RELEASE/
  variables:
    OCTEZ_PKGNAME: etherlink
    TARGET: rpm
    OCTEZ_PKGMAINTAINER: nomadic-labs
    BLST_PORTABLE: "yes"
    ARCH: amd64
  artifacts:
    name: ${TARGET}-$ARCH-$CI_COMMIT_REF_SLUG
    expire_in: 1 day
    paths:
    - packages/
    when: on_success
  parallel:
    matrix:
    - DISTRIBUTION:
      - fedora:39
      - rockylinux:9.3

eth.build:docker:kernel:
  image: ${GCP_REGISTRY}/tezos/docker-images/ci-docker:v1.10.0
  stage: prepare_release
  tags:
  - gcp
  dependencies: []
  before_script:
  - ./scripts/ci/docker_initialize.sh
  script:
  - ./scripts/ci/docker_prepare_etherlink_release.sh
  services:
  - docker:${DOCKER_VERSION}-dind
  variables:
    DOCKER_VERSION: 24.0.6
  artifacts:
    expire_in: 1 hour
    paths:
    - kernels.tar.gz

gitlab:etherlink-release:
  image: ${GCP_REGISTRY}/tezos/docker-images/ci-release:v1.4.0
  stage: publish_release_gitlab
  tags:
  - gcp
  needs:
  - eth.build:static-x86_64-linux-binaries
  - eth.build:static-arm64-linux-binaries
  - eth.build:dpkg:amd64
  - eth.build:rpm:amd64
  - eth.build:docker:kernel
  dependencies:
  - eth.build:static-x86_64-linux-binaries
  - eth.build:static-arm64-linux-binaries
  - eth.build:dpkg:amd64
  - eth.build:rpm:amd64
  - eth.build:docker:kernel
  interruptible: false
  script:
  - ./scripts/ci/create_gitlab_etherlink_package.sh
  - ./scripts/ci/create_gitlab_etherlink_release.sh
  artifacts:
    expire_in: never
    paths:
    - kernels.tar.gz
