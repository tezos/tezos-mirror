# This file was automatically generated, do not edit.
# Edit file ci/bin/main.ml instead.

stages:
- start
- build
- test
- publish

datadog_pipeline_trace:
  image: datadog/ci:v5.18.0@sha256:07475bc65fc5efec461feeaca7a16cc32c13742393db6cce5352fc5f7e1bd5a2
  stage: start
  tags:
  - gcp
  needs: []
  dependencies: []
  allow_failure: true
  timeout: 60 minutes
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - CI_MERGE_REQUEST_IID=${CI_MERGE_REQUEST_IID:-none}
  - DATADOG_SITE=datadoghq.eu datadog-ci tag --level pipeline --tags pipeline_type:$PIPELINE_TYPE
    --tags mr_number:$CI_MERGE_REQUEST_IID
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'

sdk_bindings.check_version:
  image: datadog/ci:v5.18.0@sha256:07475bc65fc5efec461feeaca7a16cc32c13742393db6cce5352fc5f7e1bd5a2
  stage: test
  tags:
  - gcp
  needs: []
  dependencies: []
  timeout: 60 minutes
  interruptible: true
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - CI_MERGE_REQUEST_IID=${CI_MERGE_REQUEST_IID:-none}
  - DATADOG_SITE=datadoghq.eu datadog-ci tag --level pipeline --tags pipeline_type:$PIPELINE_TYPE
    --tags mr_number:$CI_MERGE_REQUEST_IID
  - ./contrib/sdk-bindings/scripts/ci/check_tag_version.sh
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'

sdk_bindings.build_python_linux:
  image: ${GCP_PROTECTED_REGISTRY}/tezos/tezos/debian-rust-sdk-bindings:trixie-master-a8af426e
  stage: build
  tags:
  - gcp
  needs: []
  dependencies: []
  timeout: 60 minutes
  cache:
    key: cargo-$CI_JOB_NAME_SLUG
    paths:
    - $CI_PROJECT_DIR/.cargo/registry/cache
    - $CI_PROJECT_DIR/.cargo/registry/index
    - $CI_PROJECT_DIR/.cargo/registry/src
    policy: pull-push
  interruptible: true
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  - . ./scripts/ci/sccache-start.sh
  script:
  - export CARGO_NET_OFFLINE=false
  - . $HOME/.venv/bin/activate
  - make -C contrib/sdk-bindings/rust -f python.mk build
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  - ./scripts/ci/sccache-stop.sh
  variables:
    CARGO_INCREMENTAL: "0"
    CARGO_NET_OFFLINE: "false"
    SCCACHE_GCS_BUCKET: $GCP_SCCACHE_BUCKET
    SCCACHE_GCS_KEY_PREFIX: sccache
    SCCACHE_GCS_RW_MODE: READ_ONLY
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_IGNORE_SERVER_IO_ERROR: "1"
  artifacts:
    paths:
    - contrib/sdk-bindings/rust/target/wheels/*

sdk_bindings.build_python_macos:
  image: macos-15-xcode-16
  stage: build
  tags:
  - $TAGS
  needs: []
  dependencies: []
  timeout: 60 minutes
  cache:
    key: cargo-$CI_JOB_NAME_SLUG
    paths:
    - $CI_PROJECT_DIR/.cargo/registry/cache
    - $CI_PROJECT_DIR/.cargo/registry/index
    - $CI_PROJECT_DIR/.cargo/registry/src
    policy: pull-push
  interruptible: true
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  - . ./scripts/ci/sccache-start.sh
  script:
  - export CARGO_NET_OFFLINE=false
  - export CARGO_HOME=$HOME/.cargo
  - python3 -m venv $HOME/.venv
  - . $HOME/.venv/bin/activate
  - curl https://sh.rustup.rs -sSf | sh -s -- -y
  - . $HOME/.cargo/env
  - pip install maturin==1.5.1
  - brew install sccache
  - make -C contrib/sdk-bindings/rust -f python.mk build
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  - ./scripts/ci/sccache-stop.sh
  variables:
    CARGO_INCREMENTAL: "0"
    CARGO_NET_OFFLINE: "false"
    SCCACHE_GCS_BUCKET: $GCP_SCCACHE_BUCKET
    SCCACHE_GCS_KEY_PREFIX: sccache
    SCCACHE_GCS_RW_MODE: READ_ONLY
    SCCACHE_IDLE_TIMEOUT: "0"
    SCCACHE_IGNORE_SERVER_IO_ERROR: "1"
    TAGS: saas-macos-medium-m1
  artifacts:
    paths:
    - contrib/sdk-bindings/rust/target/wheels/*

sdk_bindings.build_python_windows:
  stage: build
  tags:
  - $TAGS
  needs: []
  dependencies: []
  timeout: 60 minutes
  interruptible: true
  before_script: []
  script:
  - '[Environment]::SetEnvironmentVariable(''CARGO_NET_OFFLINE'',''false'')'
  - '[Environment]::SetEnvironmentVariable(''CARGO_HOME'',''.cargo'')'
  - $env:Path = "$Env:CI_PROJECT_DIR\.cargo\bin;$env:Path"
  - Invoke-WebRequest -Uri https://win.rustup.rs -OutFile rustup-init.exe
  - ./rustup-init.exe -y
  - Remove-Item rustup-init.exe
  - pip install maturin==1.5.1
  - choco install make
  - make -C $Env:CI_PROJECT_DIR/contrib/sdk-bindings/rust -f python.mk build
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  variables:
    SHELL: powershell
    TAGS: saas-windows-medium-amd64
  artifacts:
    paths:
    - contrib/sdk-bindings/rust/target/wheels/*

sdk_bindings.publish:
  image: ${GCP_PROTECTED_REGISTRY}/tezos/tezos/debian-rust-sdk-bindings:trixie-master-a8af426e
  stage: publish
  tags:
  - gcp_not_interruptible
  needs:
  - sdk_bindings.check_version
  - sdk_bindings.build_python_linux
  - sdk_bindings.build_python_macos
  - sdk_bindings.build_python_windows
  dependencies:
  - sdk_bindings.build_python_linux
  - sdk_bindings.build_python_macos
  - sdk_bindings.build_python_windows
  timeout: 60 minutes
  interruptible: false
  before_script:
  - SCRIPT_STEP_BEGIN=$(date +%s)
  - . ./scripts/ci/datadog_send_job_info.sh
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'before'
  script:
  - . $HOME/.venv/bin/activate
  - make -C contrib/sdk-bindings publish
  - . ./scripts/ci/datadog_send_job_script_step_time.sh || true
  after_script:
  - . ./scripts/ci/datadog_send_job_cache_info.sh 'after'
  variables:
    MATURIN_PYPI_TOKEN: $CI_TESTPYPI_TOKEN
    MATURIN_REPOSITORY: testpypi
  retry: 0
