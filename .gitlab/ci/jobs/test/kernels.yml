test_kernels:
  extends:
    - .oc.kernels_template
  stage: test
  dependencies: []
  script:
    - make -f kernels.mk check
    - make -f kernels.mk test
  rules:
    - changes:
        - .gitlab-ci.yml
        - kernels.mk
        - src/kernel_*/**/*
        - src/risc_v/**/*
        - etherlink/kernel_evm/**/*
        - .gitlab/**/*
        - .gitlab-ci.yml
