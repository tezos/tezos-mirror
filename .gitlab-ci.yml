stages:
  - build
  - deploy
  - docker
  - manifest

fmt:
  stage: build
  image: ocaml/opam:ubuntu-22.04-ocaml-4.14
  script:
    - opam-2.1 install ocamlformat.0.25.1
    - dune build @fmt

build:
  stage: build
  image: ocaml/opam:ubuntu-22.04-ocaml-4.14
  parallel:
    matrix:
      - ARCH: ["amd64", "arm64"]
        OCTEZ_BRANCH: ["v18.1", "master"]
  tags:
    - ${ARCH}
  variables:
    # Build blst used by ocaml-bls12-381 without ADX to support old CPU
    # architectures.
    # See https://gitlab.com/tezos/tezos/-/issues/1788
    BLST_PORTABLE: "yes"
  before_script:
    - sudo apt update
  script:
    # Temporary fix: v18.1 uses caqti < 2 while master is using >= 2
    - '[[ "${OCTEZ_BRANCH}" == "v18.1" ]] || for I in 2 3 4 ; do find ./server/ -type f -name "*.ml*" -print0 | xargs -0 sed -i s/tup${I}/t${I}/g ; done;'
    - '[[ "${OCTEZ_BRANCH}" == "v18.1" ]] || find ./server/ -type f -name "*.ml*" -print0 | xargs -0 sed -i s/Caqti_lwt.Pool/Caqti_lwt_unix.Pool/g'
    - '[[ "${OCTEZ_BRANCH}" == "v18.1" ]] || find ./server/ -type f -name "*.ml*" -print0 | xargs -0 sed -i s/Caqti_lwt.connect_pool/Caqti_lwt_unix.connect_pool/g'
    - '[[ "${OCTEZ_BRANCH}" == "v18.1" ]] || find . -type f -name "dune" -print0 | xargs -0 sed -i s/caqti-lwt/caqti-lwt.unix/g'
    - git clone --depth 1 --branch $OCTEZ_BRANCH https://gitlab.com/tezos/tezos.git
    - opam-2.1 install -y --deps-only tezos/opam/virtual/octez-deps.opam
    - opam-2.1 install -y caqti-lwt caqti-driver-sqlite3 caqti-driver-postgresql ezgzip safepass --criteria="-changed,-removed"
    - dune build archiver/main.exe server/main.exe
  artifacts:
    name: binaries-${ARCH}-${OCTEZ_BRANCH}
    expire_in: 1 week
    paths:
      - archiver/main.exe
      - server/main.exe

.release:
  image: alpine
  script:
    - 'apk add zip'
    - 'apk add curl'
    - 'zip ./${ARCHIVE} archiver/main.exe server/main.exe'
    - 'curl --header "JOB-TOKEN: $CI_JOB_TOKEN" --upload-file ./${ARCHIVE} "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/teztale/${CI_COMMIT_TAG:=latest}/${ARCHIVE}"'
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_TAG != null

release:amd64:v18.1:
  stage: deploy
  extends: .release
  dependencies: ["build: [amd64, v18.1]"]
  variables:
    ARCHIVE: "binaries-amd64-v18.1.zip"

release:amd64:master:
  stage: deploy
  extends: .release
  dependencies: ["build: [amd64, master]"]
  variables:
    ARCHIVE: "binaries-amd64-master.zip"

release:arm64:v18.1:
  stage: deploy
  extends: .release
  dependencies: ["build: [arm64, v18.1]"]
  variables:
    ARCHIVE: "binaries-arm64-v18.1.zip"

release:arm64:master:
  stage: deploy
  extends: .release
  dependencies: ["build: [arm64, master]"]
  variables:
    ARCHIVE: "binaries-arm64-master.zip"

.docker:
  image: docker
  stage: docker
  tags:
    - devops
    - ${ARCH}
  before_script:
    - echo ${CI_REGISTRY_IMAGE}/{server,archiver}-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
  script:
    - docker build -t ${CI_REGISTRY_IMAGE}/server-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG} -f Dockerfile.server .
    - docker build -t ${CI_REGISTRY_IMAGE}/archiver-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG} -f Dockerfile.archiver .

.docker-push:
  extends: .docker
  script:
    - !reference [.docker, script]
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker push ${CI_REGISTRY_IMAGE}/server-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
    - docker push ${CI_REGISTRY_IMAGE}/archiver-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
  rules:
    - if: $CI_REGISTRY_IMAGE && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_REGISTRY_IMAGE && $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_REGISTRY_IMAGE
      when: manual
      allow_failure: true

docker:amd64:v18.1:
  extends: .docker-push
  dependencies: ["build: [amd64, v18.1]"]
  variables:
    ARCH: "amd64"
    OCTEZ_BRANCH: "v18.1"

docker:amd64:master:
  extends: .docker-push
  dependencies: ["build: [amd64, master]"]
  variables:
    ARCH: "amd64"
    OCTEZ_BRANCH: "master"

docker:arm64:v18.1:
  extends: .docker-push
  dependencies: ["build: [arm64, v18.1]"]
  variables:
    ARCH: "arm64"
    OCTEZ_BRANCH: "v18.1"

docker:arm64:master:
  extends: .docker-push
  dependencies: ["build: [arm64, master]"]
  variables:
    ARCH: "arm64"
    OCTEZ_BRANCH: "master"

.docker-test:
  extends: .docker
  script:
    - !reference [.docker, script]
    - docker save -o ${CI_REGISTRY_IMAGE}/server-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}.tar ${CI_REGISTRY_IMAGE}/server-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
    - docker save -o ${CI_REGISTRY_IMAGE}/archiver-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}.tar ${CI_REGISTRY_IMAGE}/archiver-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
  artifacts:
    expire_in: 1 week
    paths:
      - ${CI_REGISTRY_IMAGE}/server-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}.tar
      - ${CI_REGISTRY_IMAGE}/archiver-${ARCH}:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}.tar
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

docker-test:amd64:v18.1:
  extends: .docker-test
  dependencies: ["build: [amd64, v18.1]"]
  variables:
    ARCH: "amd64"
    OCTEZ_BRANCH: "v18.1"

.manifest:
  stage: manifest
  image: docker
  tags:
    - devops
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker manifest create ${CI_REGISTRY_IMAGE}/server:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG} `for i in amd64 arm64 ; do echo "--amend ${CI_REGISTRY_IMAGE}/server-$i:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}" ; done`
    - docker manifest push ${CI_REGISTRY_IMAGE}/server:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
    - docker manifest create ${CI_REGISTRY_IMAGE}/archiver:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG} `for i in amd64 arm64 ; do echo "--amend ${CI_REGISTRY_IMAGE}/archiver-$i:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}" ; done`
    - docker manifest push ${CI_REGISTRY_IMAGE}/archiver:${OCTEZ_BRANCH}-${CI_COMMIT_TAG:=$CI_COMMIT_REF_SLUG}
  rules:
    - if: $CI_REGISTRY_IMAGE && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_REGISTRY_IMAGE && $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_REGISTRY_IMAGE
      when: manual
      allow_failure: true

manifest:v18.1:
  extends: .manifest
  needs:
    - job: "docker:amd64:v18.1"
      artifacts: false
    - job: "docker:arm64:v18.1"
      artifacts: false
  variables:
    OCTEZ_BRANCH: "v18.1"

manifest:master:
  extends: .manifest
  needs:
    - job: "docker:amd64:master"
      artifacts: false
    - job: "docker:arm64:master"
      artifacts: false
  variables:
    OCTEZ_BRANCH: "master"
