{ parameter key_hash ;
  storage (pair timestamp (pair mutez key_hash)) ;
  code { DUP ;
         { CDR ; CAR } ;
         DUP ;
         NOW ;
         { COMPARE ; GT } ;
         IF { { UNIT ; FAILWITH } } {} ;
         SWAP ;
         DUP ;
         CAR ;
         DIP { { CDR ; CDR } } ;
         AMOUNT ;
         PAIR ;
         SWAP ;
         DIP { SWAP ; PAIR } ;
         DUP ;
         CAR ;
         AMOUNT ;
         { COMPARE ; LE } ;
         IF { { UNIT ; FAILWITH } } {} ;
         DUP ;
         CAR ;
         DIP { CDR ; IMPLICIT_ACCOUNT } ;
         UNIT ;
         TRANSFER_TOKENS ;
         NIL operation ;
         SWAP ;
         CONS ;
         PAIR } }
