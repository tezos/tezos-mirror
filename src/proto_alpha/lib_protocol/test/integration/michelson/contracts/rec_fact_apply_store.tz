{ storage (or int (lambda int int));
  parameter (or (unit %gen) (int %exec));
  code { UNPAIR;
         IF_LEFT{ DROP 2;
                  LAMBDA_REC (pair int int) int
                             { UNPAIR;
                               DUP 2;
                               EQ;
                               IF { DUP }
                                  { DUP 2;
                                    DUP 4;
                                    DUP 3;
                                    APPLY;
                                    PUSH int 1;
                                    DUP 3;
                                    SUB;
                                    EXEC;
                                    MUL};
                               DIP { DROP 3 }};
                  PUSH int 1;
                  APPLY;
                  RIGHT int}
                { DIP { ASSERT_RIGHT };
                  EXEC;
                  LEFT (lambda int int)};
         NIL operation;
         PAIR}}
