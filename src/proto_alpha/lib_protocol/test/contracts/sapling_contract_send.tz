storage (unit);
parameter (pair (contract (or sapling_transaction sapling_state)) sapling_transaction);
code { UNPAIR ;
       UNPAIR;
       SWAP ;
       SAPLING_EMPTY_STATE ;
       SWAP ;
       SAPLING_VERIFY_UPDATE ;
       ASSERT_SOME ;
       UNPAIR ;
       DROP ;
       PUSH mutez 0;
       SWAP ;
       RIGHT sapling_transaction;
       TRANSFER_TOKENS;
       NIL operation;
       SWAP;
       CONS;
       PAIR;
     }
