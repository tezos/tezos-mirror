[workspace]
resolver = "2"
members = [
  # Libraries
  "kernel_loader",
  "interpreter",
  "pvm",
  # Executables
  "sandbox",
]
exclude = ["jstz", "dummy_kernel", "tests"]

[workspace.dependencies]
rustc_apfloat = "0.2.0"
