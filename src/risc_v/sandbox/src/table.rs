use comfy_table::{presets::UTF8_FULL, Attribute, Cell, Color, ContentArrangement, Table};

use crate::commands::bench::BenchStats;

pub fn table_from_stats(stats: &BenchStats) -> Table {
    let mut table = Table::new();

    table
}
